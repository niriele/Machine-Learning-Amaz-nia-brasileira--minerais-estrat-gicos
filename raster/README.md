# Raster covariates

Place the GeoTIFF covariates required by the analysis in this directory. Raster filenames must correspond to the covariate names referenced by the tabular data and selected during modeling.

GeoTIFF files are excluded from version control because they may be large and may have separate redistribution terms.
