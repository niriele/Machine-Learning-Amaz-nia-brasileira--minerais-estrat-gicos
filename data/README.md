# Tabular input data

Place `training.csv` and `validation.csv` in this directory before running the workflow.

These datasets are not included in the initial public release. The `.gitignore` file prevents them and derived `.RData` objects from being committed accidentally.
